# Revisions to skip when running git blame.
#
# Enable it once per clone:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# It is not automatic: blame.ignoreRevsFile is local config, so a fresh clone
# ignores this file until that command is run.
#
# Only whole-tree, no-logic commits belong here. Each entry must be a full
# 40-character SHA.

# Apply .clang-format to the whole tree (78 files). Verified to change nothing
# but layout: every object file built from the tree is bit-identical before and
# after.
0744fd7dba1e5a19ea2bcdda2b5367c53b41ed6c

# Indent namespace bodies (66 files). Same verification: every object file
# built from the tree is bit-identical before and after.
b20192f49640a308bd79f81a06f15755b2c3d5b6
